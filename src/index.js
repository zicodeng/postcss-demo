import React from 'react';
import ReactDOM from 'react-dom';

// Global style should only be imported once.
import './shared/stylesheets/global.css';

import App from 'App.js';

ReactDOM.render(<App />, document.getElementById('app'));
